import { ThemeSimposioPipe } from './theme-simposio.pipe';

describe('ThemeSimposioPipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeSimposioPipe();
    expect(pipe).toBeTruthy();
  });
});
