import { DayAndMonthPipe } from './day-and-month.pipe';

describe('DayAndMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new DayAndMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
