import { ModalitiesPipe } from './modalities.pipe';

describe('ModalitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ModalitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
