import { ModalitiesWorkPipe } from './modalities-work.pipe';

describe('ModalitiesWorkPipe', () => {
  it('create an instance', () => {
    const pipe = new ModalitiesWorkPipe();
    expect(pipe).toBeTruthy();
  });
});
